# Ginger scaffold metadata, retained for ginger doctor.
# Runtime settings use the per-user config.yaml loaded by internal/config.
app:
  name: kubePeep
  env: production
  version: 0.1.0

http:
  host: 127.0.0.1
  port: 2748
  shutdown_timeout: 10

log:
  level: info
  format: json
